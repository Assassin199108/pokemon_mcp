[project]
name = "pokemon-mcp"
version = "0.1.0"
description = "Pokemon MCP server for querying Pokemon information via PokéAPI"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.25.0",
]
