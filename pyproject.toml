[project]
name = "pokemon-mcp"
version = "0.1.0"
description = "Pokemon MCP server for querying Pokemon information via PokéAPI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "httpx>=0.25.0",
]
